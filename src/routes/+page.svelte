<!--
	Unreachable in practice: the root layout's auth guard redirects here to
	either /expenses or /login before this ever renders. Kept empty rather
	than deleted, since SvelteKit requires a +page.svelte for the route.
-->
